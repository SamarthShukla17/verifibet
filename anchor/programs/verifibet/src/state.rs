//! On-chain account state (parimutuel Market, Bet PDAs, etc).
//!
//! Empty for now — wired into lib.rs so later sessions add account structs
//! here rather than inline in instructions/. PDA seeds derived here must
//! match lib/solana/pda.ts exactly (see CLAUDE.md conventions).
